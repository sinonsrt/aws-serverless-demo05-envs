{
  "name": "lambda-environment",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "invoke-local": "sls invoke local --stage prod -f commit-message-scheduler --l",
    "invoke": "sls invoke -f commit-message-scheduler --l"
  },
  "license": "MIT",
  "dependencies": {
    "aws-sdk": "^2.1364.0",
    "axios": "^1.3.6",
    "cheerio": "^1.0.0-rc.12",
    "env-var": "^7.3.1",
    "uuid": "^9.0.0"
  }
}
